services:
  - type: web
    name: event-registration-system
    rootDir: CodeAlpha_Backend_Development_Tasks/CodeAlpha_Event_registration_system
    plan: free
    env: node
    buildCommand: npm run install-all && npm run build:frontend
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NODE_VERSION
        value: "20"
      - key: VITE_API_URL
        value: /api
      - key: DATABASE_URL
        fromDatabase:
          name: event-db
          property: connectionString

databases:
  - name: event-db
    databaseName: eventsdb
    user: events_admin
    plan: free
