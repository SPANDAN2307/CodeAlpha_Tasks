# 🔗 Custom URL Shortener 
> **CodeAlpha Backend Development — Task 3**

A comprehensive, full-stack REST API URL shortener application built using Node.js, Express, and mapped securely into an embedded SQLite storage system structure ensuring perfectly persistent deterministic routing.

---

## 🌟 Overview
Engineered to resolve complex domain inputs into secure 6-character strings directly bridging HTTP 302 redirection architecture avoiding complicated external routing proxies successfully mapping massive path blocks dynamically.

## ✨ Highlighted Features
* **Deterministic String Generation:** Resolves random array combinations mapping precisely unique string blocks avoiding primary key collisions completely natively.
* **Smart HTTP Routing Detection:** Automatically intercepts non-compliant syntax structures pre-pending identical networking protocols automatically dynamically handling syntax logic blocks.
* **Persistent Internal Logging:** Secure SQL arrays completely mapping exact creation temporal arrays natively.

## 🛠️ Technology Stack
| Technology | Architecture Concept |
|----------|-------------------|
| API Routing System | Node.js, Express.js |
| HTTP Transport Protocols | CORS middleware configurations |
| Database Engine | SQLite3 (Serverless Embedded Node Engine) |

## 🚀 Quick Start Setup

### The Accelerated Mechanism (Windows)
```batch
# Launch the autonomous configuration sequence resolving parameters natively securely opening localhost:3000
run_server.bat
```

### Manual Node Extraction Schema
```bash
# 1. Resolve package configuration arrays locally capturing missing node parameters automatically securely via standard commands natively configuring the root
npm add

# 2. Trigger active node connection blocks directly resolving index path successfully without UI dependencies required
node server.js
```

## 🔌 Core API Architecture

### 🔗 Generate Link Objects 
`POST /api/shorten` — Securely evaluates explicit variable payloads constructing deterministic shortened path domains intercepting bad HTTP structures prior.

**Target Payload:**
```json
{
  "url": "https://www.google.com/"
}
```

### 🔀 Resolver Endpoint 
`GET /:code` — Evaluates HTTP queries linking specific 6-character arrays natively to corresponding SQL nodes, resolving dynamic 302 Redirection Headers instantaneously intercepting HTTP targets reliably directly mapped inside active parameter blocks.
